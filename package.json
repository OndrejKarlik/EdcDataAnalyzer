{
    "name": "edc-report-analyzer",
    "version": "1.0.0",
    "main": "EdcReportAnalyzer.js",
    "type": "module",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "clean": "tsc --build --clean",
        "build:ts": "tsc --build",
        "build:less": "lessc EdcReportAnalyzer.less EdcReportAnalyzer.css",
        "build": "npm run build:less && npm run build:ts"
    },
    "dependencies": {
        "lessc": "^1.0.2",
        "typescript": "^5.8.2"
    }
}
